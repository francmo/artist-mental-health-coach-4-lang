{
  "name": "creative-wellness-coach",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:ita": "vite build --config vite.config.ita.js",
    "build:eng": "vite build --config vite.config.eng.js",
    "build": "npm run build:ita && npm run build:eng",
    "preview:ita": "vite preview --config vite.config.ita.js",
    "preview:eng": "vite preview --config vite.config.eng.js"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "vite": "^5.1.0"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  }
}
